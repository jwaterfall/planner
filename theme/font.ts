import theme from 'styled-theming';

export const weight1 = theme('mode', {
  light: 500,
  dark: 400,
});

export const weight2 = theme('mode', {
  light: 600,
  dark: 600,
});

export const weight3 = theme('mode', {
  light: 700,
  dark: 500,
});
